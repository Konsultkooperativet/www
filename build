cd cv
npm install -g resume-cli
#npm install -g jsonresume-theme-stackoverflow
npm install ./jsonresume-theme-konsultcoop

for f in consultants/*.json; 
do 
	echo "Processing $f file.."; 

	cp $f resume.json
	resume export --theme stackoverflow $f.html
	cp $f.html ../public/cv/$f.html
done

